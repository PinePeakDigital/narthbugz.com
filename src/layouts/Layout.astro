<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <script is:inline>
      // Immediately set the theme before the page renders
      if (
        localStorage.getItem("darkMode") === "true" ||
        (!localStorage.getItem("darkMode") &&
          window.matchMedia("(prefers-color-scheme: dark)").matches)
      ) {
        document.documentElement.classList.add("dark");
      }
    </script>
    <title>Narthbugz - Evidence-Based Time Tracking for Dev Teams</title>
    <meta
      name="description"
      content="Track development time and forecast project completion with confidence using Monte Carlo simulations."
    />
    <script is:inline>
      // Immediately set the theme before the page renders
      if (
        localStorage.getItem("darkMode") === "true" ||
        (!localStorage.getItem("darkMode") &&
          window.matchMedia("(prefers-color-scheme: dark)").matches)
      ) {
        document.documentElement.classList.add("dark");
      }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <nav>
      <a href="/" class="logo">Narthbugz</a>
      <button id="theme-toggle" aria-label="Toggle dark mode">
        <svg
          class="sun"
          xmlns="http://www.w3.org/2000/svg"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <circle cx="12" cy="12" r="5"></circle>
          <line x1="12" y1="1" x2="12" y2="3"></line>
          <line x1="12" y1="21" x2="12" y2="23"></line>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
          <line x1="1" y1="12" x2="3" y2="12"></line>
          <line x1="21" y1="12" x2="23" y2="12"></line>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
        </svg>
        <svg
          class="moon"
          xmlns="http://www.w3.org/2000/svg"
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
        </svg>
      </button>
      <div class="nav-links">
        <a href="/features">Features</a>
        <a href="/how-it-works">How It Works</a>
        <a href="/pricing">Pricing</a>
        <a href="https://docs.narthbugz.com">Docs</a>
        <a href="#login" class="button secondary">Login</a>
        <a href="#signup" class="button primary">Sign Up</a>
      </div>
    </nav>
    <slot />
    <footer>
      <div class="footer-content">
        <div class="footer-section">
          <h3>Product</h3>
          <a href="/features">Features</a>
          <a href="/pricing">Pricing</a>
          <a href="/how-it-works">How It Works</a>
          <a href="https://docs.narthbugz.com">Documentation</a>
        </div>
        <div class="footer-section">
          <h3>Company</h3>
          <a href="#about">About Us</a>
          <a href="#blog">Blog</a>
          <a href="#careers">Careers</a>
          <a href="#contact">Contact</a>
        </div>
        <div class="footer-section">
          <h3>Legal</h3>
          <a href="#privacy">Privacy Policy</a>
          <a href="#terms">Terms of Service</a>
          <a href="#security">Security</a>
        </div>
        <div class="footer-section">
          <h3>Connect</h3>
          <a href="#twitter">Twitter</a>
          <a href="#linkedin">LinkedIn</a>
          <a href="#github">GitHub</a>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2024 Narthbugz. All rights reserved.</p>
      </div>
    </footer>
    <script>
      // Remove the initial theme setting script since it's now handled inline
      const themeToggle = document.getElementById("theme-toggle");
      themeToggle?.addEventListener("click", () => {
        document.documentElement.classList.toggle("dark");
        localStorage.setItem(
          "darkMode",
          document.documentElement.classList.contains("dark").toString()
        );
      });
    </script>
  </body>
</html>

<style>
  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 2rem;
    max-width: 1200px;
    margin: 0 auto;
    background: var(--bg-primary);
    transition: background-color var(--transition-speed) ease;
    backdrop-filter: blur(10px);
    border-radius: 1rem;
    margin-top: 1rem;
    position: sticky;
    top: 1rem;
    z-index: 100;
    box-shadow: var(--shadow);
  }

  .logo {
    font-size: 1.75rem;
    font-weight: 800;
    transition: background-color var(--transition-speed) ease;
    background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    position: relative;
    letter-spacing: -0.02em;
  }

  .logo::after {
    content: "";
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .logo:hover::after {
    transform: scaleX(1);
  }

  .nav-links {
    display: flex;
    gap: 2rem;
    align-items: center;
  }

  .nav-links a {
    color: var(--text-primary);
    text-decoration: none;
    font-weight: 500;
    transition: all 0.2s;
    position: relative;
    font-size: 1.0625rem;
    letter-spacing: -0.01em;
  }

  .nav-links a:not(.button)::after {
    content: "";
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .nav-links a:not(.button):hover::after {
    transform: scaleX(1);
  }

  .button {
    padding: 0.875rem 1.75rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.2s;
    font-size: 1.0625rem;
    letter-spacing: -0.01em;
  }

  .button.primary,
  .nav-links .button.primary {
    background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
    color: white;
    box-shadow:
      0 4px 6px -1px rgba(0, 0, 0, 0.1),
      0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }

  .button.primary:hover {
    transform: translateY(-2px);
    box-shadow:
      0 10px 15px -3px rgba(0, 0, 0, 0.1),
      0 4px 6px -2px rgba(0, 0, 0, 0.05);
  }

  .button.secondary {
    border: 1px solid #3245ff;
    color: #3245ff;
  }

  .button.secondary:hover {
    background: rgba(50, 69, 255, 0.1);
  }

  @media (max-width: 768px) {
    nav {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
      margin: 1rem;
      position: relative;
      top: 0;
    }

    .nav-links {
      flex-direction: column;
      gap: 1rem;
    }

    .footer-content {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }
  }

  footer {
    background: var(--bg-primary);
    backdrop-filter: blur(10px);
    padding: 4rem 2rem 2rem;
    margin-top: 4rem;
  }

  .footer-content {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 4rem;
  }

  .footer-section h3 {
    color: var(--text-primary);
    font-size: 1.125rem;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .footer-section {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .footer-section a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.2s;
    font-size: 1rem;
  }

  .footer-section a:hover {
    color: #3245ff;
  }

  .footer-bottom {
    max-width: 1200px;
    margin: 0 auto;
    padding-top: 2rem;
    margin-top: 2rem;
    border-top: 1px solid rgba(75, 85, 99, 0.2);
    text-align: center;
    color: var(--text-secondary);
    font-size: 0.875rem;
  }
  @property --bg-image {
    syntax: "<image>";
    initial-value: url("/src/assets/background.svg");
    inherits: false;
  }

  :root {
    --transition-speed: 0.2s;
    --text-primary: #111827;
    --text-secondary: #4b5563;
    --bg-primary: rgba(255, 255, 255, 0.8);
    --bg-secondary: rgba(255, 255, 255, 0.9);
    --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1),
      0 2px 4px -1px rgba(0, 0, 0, 0.06);
    --gradient: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
    --bg-color: #ffffff;
    --gradient-bg: linear-gradient(
      135deg,
      rgba(50, 69, 255, 0.03) 0%,
      rgba(188, 82, 238, 0.03) 100%
    );
    --bg-opacity: 0.7;
    --bg-image: url("/src/assets/background.svg");
    --gradient-opacity: 0.05;
  }

  .dark {
    --text-primary: #f3f4f6;
    --text-secondary: #d1d5db;
    --bg-primary: rgba(17, 24, 39, 0.85);
    --bg-secondary: rgba(17, 24, 39, 0.95);
    --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.2),
      0 2px 4px -1px rgba(0, 0, 0, 0.12);
    --bg-color: #0a0f1c;
    --gradient-bg: linear-gradient(
      135deg,
      rgba(50, 69, 255, 0.05) 0%,
      rgba(188, 82, 238, 0.05) 100%
    );
    --bg-opacity: 0.5;
    --bg-image: url("/src/assets/background.svg#dark");
    --gradient-opacity: 0.1;
  }

  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
    font-family:
      "Inter",
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
    background: linear-gradient(
      135deg,
      rgba(50, 69, 255, var(--gradient-opacity)) 0%,
      rgba(188, 82, 238, var(--gradient-opacity)) 100%
    ), var(--bg-color);
    position: relative;
    overflow-x: hidden;
  }

  body::before {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: var(--bg-image);
    background-size: cover;
    background-position: center;
    opacity: var(--bg-opacity);
    z-index: -1;
    animation: pulse 15s ease-in-out infinite;
  }

  @keyframes pulse {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
    100% {
      transform: scale(1);
    }
  }

  #theme-toggle {
    background: none;
    border: none;
    padding: 0.5rem;
    cursor: pointer;
    color: var(--text-primary);
    margin-right: 1rem;
    border-radius: 0.5rem;
    transition: background-color 0.2s;
  }

  #theme-toggle:hover {
    background: rgba(50, 69, 255, 0.1);
  }

  #theme-toggle .moon {
    display: none;
  }

  .dark #theme-toggle .sun {
    display: none;
  }

  .dark #theme-toggle .moon {
    display: block;
  }
</style>
