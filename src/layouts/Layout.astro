<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Narthbugz - Evidence-Based Time Tracking for Dev Teams</title>
		<meta name="description" content="Track development time and forecast project completion with confidence using Monte Carlo simulations." />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
	</head>
	<body>
		<nav>
			<div class="logo">Narthbugz</div>
			<div class="nav-links">
				<a href="/features">Features</a>
				<a href="/how-it-works">How It Works</a>
				<a href="/pricing">Pricing</a>
				<a href="https://docs.narthbugz.com">Docs</a>
				<a href="#login" class="button secondary">Login</a>
				<a href="#signup" class="button primary">Sign Up</a>
			</div>
		</nav>
		<slot />
	</body>
</html>

<style>
	nav {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 1.5rem 2rem;
		max-width: 1200px;
		margin: 0 auto;
		background: rgba(255, 255, 255, 0.8);
		backdrop-filter: blur(10px);
		border-radius: 1rem;
		margin-top: 1rem;
		position: sticky;
		top: 1rem;
		z-index: 100;
		box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
	}

	.logo {
		font-size: 1.75rem;
		font-weight: 800;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		position: relative;
		letter-spacing: -0.02em;
	}

	.logo::after {
		content: '';
		position: absolute;
		bottom: -4px;
		left: 0;
		width: 100%;
		height: 2px;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.logo:hover::after {
		transform: scaleX(1);
	}

	.nav-links {
		display: flex;
		gap: 2rem;
		align-items: center;
	}

	.nav-links a {
		color: #111827;
		text-decoration: none;
		font-weight: 500;
		transition: all 0.2s;
		position: relative;
		font-size: 1.0625rem;
		letter-spacing: -0.01em;
	}

	.nav-links a:not(.button)::after {
		content: '';
		position: absolute;
		bottom: -4px;
		left: 0;
		width: 100%;
		height: 2px;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.nav-links a:not(.button):hover::after {
		transform: scaleX(1);
	}

	.button {
		padding: 0.875rem 1.75rem;
		border-radius: 0.5rem;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.2s;
		font-size: 1.0625rem;
		letter-spacing: -0.01em;
	}

	.button.primary {
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		color: white;
		box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
	}

	.button.primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
	}

	.button.secondary {
		border: 1px solid #3245ff;
		color: #3245ff;
	}

	.button.secondary:hover {
		background: rgba(50, 69, 255, 0.1);
	}

	@media (max-width: 768px) {
		nav {
			flex-direction: column;
			gap: 1rem;
			text-align: center;
			margin: 1rem;
			position: relative;
			top: 0;
		}

		.nav-links {
			flex-direction: column;
			gap: 1rem;
		}
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
		-webkit-font-smoothing: antialiased;
		-moz-osx-font-smoothing: grayscale;
		text-rendering: optimizeLegibility;
	}
</style>
