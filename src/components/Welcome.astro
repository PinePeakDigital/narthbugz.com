---
import background from '../assets/background.svg';
---

<div id="container">
	<img id="background" src={background.src} alt="" fetchpriority="high" />
	<main>
		<nav>
			<div class="logo">Narthbugz</div>
			<div class="nav-links">
				<a href="/features">Features</a>
				<a href="/how-it-works">How It Works</a>
				<a href="#pricing">Pricing</a>
				<a href="https://docs.narthbugz.com">Docs</a>
				<a href="#login" class="button secondary">Login</a>
				<a href="#signup" class="button primary">Sign Up</a>
			</div>
		</nav>

		<section id="hero">
			<h1>Time Tracking That Actually Predicts The Future</h1>
			<p class="subtitle">
				Stop guessing at deadlines. Use evidence-based forecasting powered by Monte Carlo simulations
				to predict completion dates with confidence intervals.
			</p>
			<div id="cta-buttons">
				<a href="#demo" class="button primary">Start Free Trial</a>
				<a href="#learn-more" class="button secondary">Watch Demo</a>
			</div>
			<div class="stats">
				<div class="stat">
					<span class="number">93%</span>
					<span class="label">More Accurate Estimates</span>
				</div>
				<div class="stat">
					<span class="number">2.5x</span>
					<span class="label">Faster Planning</span>
				</div>
				<div class="stat">
					<span class="number">10k+</span>
					<span class="label">Dev Teams</span>
				</div>
			</div>
		</section>

		<section id="features">
			<h2>Features Built for Dev Teams</h2>
			<div class="features-grid">
				<div class="feature">
					<div class="feature-icon">⏱️</div>
					<h3>Automated Time Tracking</h3>
					<p>Integrates with your IDE and Git workflow to automatically track time spent on tasks.</p>
				</div>
				<div class="feature">
					<div class="feature-icon">🎲</div>
					<h3>Monte Carlo Forecasting</h3>
					<p>Use historical data to simulate thousands of possible futures and make better predictions.</p>
				</div>
				<div class="feature">
					<div class="feature-icon">📊</div>
					<h3>Team Analytics</h3>
					<p>Understand your team's velocity and identify bottlenecks with powerful analytics.</p>
				</div>
				<div class="feature">
					<div class="feature-icon">🔄</div>
					<h3>JIRA Integration</h3>
					<p>Seamlessly sync with JIRA for automatic task tracking and time logging.</p>
				</div>
			</div>
		</section>

		<section id="how-it-works">
			<h2>How It Works</h2>
			<div class="steps">
				<div class="step">
					<div class="step-number">1</div>
					<h3>Connect Your Tools</h3>
					<p>Integrate with your existing development tools and project management software.</p>
				</div>
				<div class="step">
					<div class="step-number">2</div>
					<h3>Track Time</h3>
					<p>Automatically collect time data as your team works on tasks.</p>
				</div>
				<div class="step">
					<div class="step-number">3</div>
					<h3>Generate Forecasts</h3>
					<p>Get accurate predictions based on your team's actual performance data.</p>
				</div>
			</div>
		</section>
	</main>
</div>

<style>
	#background {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
		filter: blur(100px);
		opacity: 0.7;
	}

	#container {
		min-height: 100vh;
		color: #111827;
	}

	nav {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 1.5rem 2rem;
		max-width: 1200px;
		margin: 0 auto;
		background: rgba(255, 255, 255, 0.8);
		backdrop-filter: blur(10px);
		border-radius: 1rem;
		margin-top: 1rem;
		position: sticky;
		top: 1rem;
		z-index: 100;
		box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
	}

	.logo {
		font-size: 1.75rem;
		font-weight: 800;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		position: relative;
		letter-spacing: -0.02em;
	}

	.logo::after {
		content: '';
		position: absolute;
		bottom: -4px;
		left: 0;
		width: 100%;
		height: 2px;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.logo:hover::after {
		transform: scaleX(1);
	}

	.nav-links {
		display: flex;
		gap: 2rem;
		align-items: center;
	}

	.nav-links a {
		color: #111827;
		text-decoration: none;
		font-weight: 500;
		transition: all 0.2s;
		position: relative;
		font-size: 1.0625rem;
		letter-spacing: -0.01em;
	}

	.nav-links a:not(.button)::after {
		content: '';
		position: absolute;
		bottom: -4px;
		left: 0;
		width: 100%;
		height: 2px;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	.nav-links a:not(.button):hover::after {
		transform: scaleX(1);
	}

	main {
		max-width: 1200px;
		margin: 0 auto;
		padding: 2rem;
	}

	#hero {
		text-align: center;
		padding: 6rem 0 4rem;
	}

	h1 {
		font-size: 4rem;
		font-weight: 800;
		margin: 0;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		line-height: 1.1;
		letter-spacing: -0.02em;
		max-width: 800px;
		margin: 0 auto;
	}

	h2 {
		font-size: 2.75rem;
		text-align: center;
		margin-bottom: 3rem;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		font-weight: 700;
		letter-spacing: -0.01em;
		line-height: 1.2;
	}

	.subtitle {
		font-size: 1.375rem;
		color: #4b5563;
		max-width: 600px;
		margin: 1.75rem auto;
		line-height: 1.6;
		letter-spacing: -0.01em;
		font-weight: 400;
	}

	.stats {
		display: flex;
		justify-content: center;
		gap: 4rem;
		margin-top: 4rem;
		padding: 2rem;
		background: rgba(255, 255, 255, 0.8);
		backdrop-filter: blur(10px);
		border-radius: 1rem;
		box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
	}

	.stat {
		display: flex;
		flex-direction: column;
		align-items: center;
		transition: transform 0.3s ease;
	}

	.stat:hover {
		transform: translateY(-5px);
	}

	.stat .number {
		font-size: 3rem;
		font-weight: 700;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		letter-spacing: -0.02em;
		line-height: 1;
	}

	.stat .label {
		color: #4b5563;
		margin-top: 0.75rem;
		font-weight: 500;
		font-size: 1.125rem;
		letter-spacing: -0.01em;
	}

	#cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
		margin-top: 2rem;
	}

	.button {
		padding: 0.875rem 1.75rem;
		border-radius: 0.5rem;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.2s;
		font-size: 1.0625rem;
		letter-spacing: -0.01em;
	}

	.button.primary {
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		color: white;
		box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
	}

	.button.primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
	}

	.button.secondary {
		border: 1px solid #3245ff;
		color: #3245ff;
	}

	.button.secondary:hover {
		background: rgba(50, 69, 255, 0.1);
	}

	.features-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		padding: 2rem 0;
	}

	.feature {
		background: rgba(255, 255, 255, 0.8);
		padding: 2rem;
		border-radius: 1rem;
		backdrop-filter: blur(10px);
		transition: all 0.3s ease;
		box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
	}

	.feature:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
	}

	.feature-icon {
		font-size: 2rem;
		margin-bottom: 1rem;
	}

	.feature h3 {
		margin: 0 0 1rem 0;
		font-size: 1.25rem;
		color: #111827;
		font-weight: 600;
		letter-spacing: -0.01em;
		line-height: 1.4;
	}

	.feature p {
		margin: 0;
		color: #4b5563;
		line-height: 1.6;
		font-size: 1.0625rem;
	}

	#how-it-works {
		padding: 6rem 0;
	}

	.steps {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.step {
		text-align: center;
		padding: 2rem;
		transition: transform 0.3s ease;
	}

	.step:hover {
		transform: translateY(-5px);
	}

	.step-number {
		width: 3rem;
		height: 3rem;
		background: linear-gradient(83.21deg, #3245ff 0%, #bc52ee 100%);
		color: white;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: bold;
		font-size: 1.5rem;
		margin: 0 auto 1.5rem;
		transition: transform 0.3s ease;
	}

	.step:hover .step-number {
		transform: scale(1.1);
	}

	@media (max-width: 768px) {
		h1 {
			font-size: 2.5rem;
		}

		h2 {
			font-size: 2rem;
		}

		nav {
			flex-direction: column;
			gap: 1rem;
			text-align: center;
			margin: 1rem;
			position: relative;
			top: 0;
		}

		.nav-links {
			flex-direction: column;
			gap: 1rem;
		}

		#cta-buttons {
			flex-direction: column;
		}

		.button {
			width: 100%;
			text-align: center;
		}

		.stats {
			flex-direction: column;
			gap: 2rem;
			padding: 1rem;
		}

		.steps {
			grid-template-columns: 1fr;
		}
	}
</style>
